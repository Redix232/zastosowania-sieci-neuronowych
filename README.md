# llm-gemini-app

## Configuration

Create `.streamlit/secrets.toml` with the Gemini connection details:

```toml
API_KEY = "your-gemini-api-key"
BASE_URL = "https://generativelanguage.googleapis.com/v1beta/openai/"
```

The app uses `gemini-2.5-flash` by default.